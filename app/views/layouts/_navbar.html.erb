<nav class="navbar header is-fixed-top" role="navigation" aria-label="main navigation">
  <div class="navbar-brand">
    <%= link_to channels_path, class: 'navbar-item' do%>
      <h1 class="title is-4">GroupChat</h1>
    <%end%>

    <a role="button" class="navbar-burger burger" aria-label="menu" aria-expanded="false" data-target="navbarBasicExample">
      <span aria-hidden="true"></span>
      <span aria-hidden="true"></span>
      <span aria-hidden="true"></span>
    </a>
  </div>

  <div id="navbarBasicExample" class="navbar-menu">
    <div class="navbar-end">
      <div class="navbar-item">
        <div class="buttons">
          <% if current_user.nil? %>
            <%= link_to raw('<strong>Sign up</strong>'), 
              new_user_registration_path,
              class: 'button is-primary is-small'%>
            <%= link_to 'Log in',
                new_user_session_path,
                class: 'button is-light is-small'%>
          <% else %>
            <%= link_to raw('<strong>Log out</strong>'), 
              destroy_user_session_path,
              class: 'button is-primary is-small',
              method: :delete%>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</nav>